import React from 'react'

function placesList() {
    

    let listEl = document.getElementById('placesList');

    console.log("placesList:ListEl = ", listEl);

    return (
        <div style={{ display: 'flex', justifyContent: 'flex-end' }}>
                    ava
        </div>
    )
}

export default placesList
